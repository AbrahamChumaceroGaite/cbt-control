name: CD — Deploy to DEV

on:
  push:
    branches: [develop]

concurrency:
  group: deploy-develop
  cancel-in-progress: true

jobs:
  notify:
    name: Deploy DEV
    runs-on: ubuntu-latest
    steps:
      - name: Deployment handled by Coolify
        run: echo "Push to develop received — Coolify picks up from here if configured for this branch."
