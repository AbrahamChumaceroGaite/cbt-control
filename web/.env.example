API_URL=http://localhost:4001

# WebSocket URL — only needed in development (direct to NestJS port)
# In production (nginx), leave empty: socket.io uses same origin
NEXT_PUBLIC_WS_URL=http://localhost:4001
